---
name: Bug report
about: Something broke — a trace didn't parse, resolve, or render
labels: bug
---

## The raw trace

<!-- Paste the EXACT trace/log text (sanitize usernames/hosts/paths, but keep the
     structure byte-accurate — whitespace and prefixes matter). This is the most
     important part of the report: it becomes a test fixture. -->

```text

```

## What happened / what you expected

<!-- e.g. "exit 2, but there's clearly a traceback in there" or
     "frame X resolved to the wrong function" -->

## Environment

- crashpath version (`npx crashpath --help` header):
- OS / Node version:
- Language of the trace (python / js / other):
