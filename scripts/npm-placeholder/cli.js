#!/usr/bin/env node
console.log(
  [
    "crashpath — paste a stack trace, see the failure as a path through your codebase.",
    "",
    "v0.1 is in active development. Watch the repo for the first working release:",
    "  https://github.com/hq525/Trace-Visualizer",
  ].join("\n"),
);
